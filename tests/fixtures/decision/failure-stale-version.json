{
  "$comment": "Schema-valid decision input — VERSION_CONFLICT is a domain-level conflict, not a schema violation. Core state-machine test asserts the conflict behavior.",
  "valid": true,
  "data": {
    "nodeId": "link-click",
    "choiceId": "verify",
    "expectedVersion": 3
  }
}
