# SOPscape Council 项目详细说明

> 目标读者：第一次加入项目的开发者、设计者、评审者和合作伙伴。
> 当前阶段：产品设计与开发准备，应用源码尚未开始实现。本文描述的是已确认的产品目标和技术边界。

## 1. 一句话说明

SOPscape Council 把一份静态 SOP 交给三名职责互补的 AI 专家审议，再由主持人形成可追溯的共识、分歧、证据缺口与决策节点，最终生成用户可以亲自选择并看到后果的 3D 情境演练和结构化报告。

它不是：

- 只把 SOP 缩短成摘要的工具；
- 让多个 AI 无限制互相聊天的聊天室；
- 与业务结果无关的 3D 展示页面；
- 让模型直接生成 JavaScript、Three.js 场景代码或任意资源；
- 只服务加密货币用户的产品。

## 2. 项目为什么存在

企业和团队通常已经拥有大量 SOP，但“文档存在”不等于“流程真正可执行”。传统 SOP 常见问题包括：

- 阅读者记住了文字，却不知道压力场景下该如何判断；
- 步骤之间的责任、前置条件和异常路径不清楚；
- 同一条规则被不同岗位理解成不同含义；
- 某些结论听起来合理，却无法指回原始 SOP 中的证据；
- 培训只考记忆，没有展示错误选择会造成什么后果；
- 普通单模型总结容易给出一个流畅答案，但把分歧和不确定性隐藏起来。

SOPscape Council 将 SOP 从“被动阅读材料”变成“可审议、可演练、可验证的决策系统”。用户不只是看答案，而是能看到不同专业视角、理解哪些结论证据充分、在关键节点作出选择，并观察选择如何改变风险和后续行动。

## 3. 谁会使用它

首批目标用户包括：

- 企业培训与安全意识团队；
- 流程负责人和业务运营团队；
- 合规、审计和风控人员；
- 新员工和需要接受流程训练的成员；
- 希望验证 SOP 是否完整、清楚且可执行的管理者；
- 通过 OKX.AI 或标准 MCP 调用专业流程分析能力的 AI Agent。

比赛版使用“钓鱼邮件处置”作为唯一要求完整打磨的代表场景，但底层领域结构是通用的。后续可以应用于客服升级、生产事故、运维故障、审批、数据泄漏响应、门店安全和医疗行政流程等场景。

## 4. 输入和输出

### 4.1 输入

用户提交：

- SOP 标题；
- 最多约 15,000 个中文字符、60,000 UTF-8 字节的正文；
- 可选语言和场景元数据。

超出模型直接分析阈值时，系统会执行一次受控压缩，同时保留步骤 ID、角色、条件和证据引用。压缩不是自由摘要，也不能删除后续分析需要的关键信息。

### 4.2 输出

一次成功演练会产生：

1. SOP 的步骤、角色、条件和责任；
2. 风险、异常路径和失败后果；
3. 每项结论对应的证据引用；
4. 无法从 SOP 证明的判断；
5. 三名专家的共识、分歧和证据缺口；
6. 推荐路径和关键决策节点；
7. 每个选择对应的业务后果和下一步；
8. Schema 校验通过的 `scene.json`；
9. 结构化报告；
10. 由权限保护的只读分享链接。

模型只生成受白名单和 Schema 约束的数据。模型输出不能包含可执行代码、任意远程资源地址或直接操作 WebGL 的指令。

## 5. 一次完整用户体验

以下用钓鱼邮件场景说明完整流程。

### 第一步：提交 SOP

用户粘贴《收到疑似钓鱼邮件后的处置 SOP》，点击“开始演练”。服务端校验大小、结构、频率和预算，创建一个 Rehearsal。

### 第二步：立即进入指挥室

Web 不等待模型全部完成才显示页面。目标是在约两秒内显示可操作的固定全息指挥室、文本状态和进度区域，让用户明确知道系统正在做什么。

### 第三步：三名专家并行分析

系统同时启动三条结构化分析：

- 流程分析员拆解步骤、角色、条件和责任；
- 风险挑战者寻找危险动作、失败链路和边缘情况；
- 证据审计员核对每项判断能否指回原始 SOP。

3D 指挥室中对应的专家席位和进度状态由真实后台阶段驱动，不使用独立计时器伪造进度。

### 第四步：主持人形成议会结果

只有三份专家输出全部通过 Schema 校验，主持人才会开始工作。主持人不会重新替专家分析，而是：

- 合并已达成的共识；
- 保留没有解决的分歧；
- 标记证据缺口；
- 形成推荐路径；
- 生成用户可选择的决策节点；
- 输出可渲染的 SceneDocument 和报告。

任何一名专家最终失败，任务进入失败状态，不会拿两份结果假装形成完整共识。

### 第五步：用户作出真实决策

例如用户看到两个选项：

- 直接点击邮件中的密码重置链接；
- 不点击链接，通过独立渠道核验发件人并上报安全团队。

用户选择后，服务端以版本控制方式写入权威状态，再返回新的 DecisionResult。

### 第六步：业务状态和 3D 同时变化

选择会真实改变：

- 专家置信度；
- 风险路径拓扑；
- 当前后果和下一步行动；
- 场景颜色语义；
- 镜头关注点；
- 报告中的决策记录。

这使 3D 成为业务状态的可视化界面，而不是播放完就结束的动画。

### 第七步：报告和分享

用户查看共识、分歧、证据缺口、选择过程和最终建议，并可创建一个独立的只读分享链接。获得分享链接的人只能查看安全投影，不能作决策、取消任务、创建新分享或读取内部元数据。

## 6. 三名专家和主持人的职责

### 6.1 Procedure Analyst：流程分析员

核心问题：SOP 要求谁在什么条件下做什么？

主要产出：

- 步骤顺序；
- 责任角色；
- 前置条件和触发条件；
- 缺失或含糊的流程节点；
- 可以转换成训练选择的决策点。

### 6.2 Risk Challenger：风险挑战者

核心问题：哪一步可能失败、被误用或造成严重后果？

主要产出：

- 高风险动作；
- 异常路径；
- 用户常见错误；
- 风险严重程度；
- 错误选择的后果链。

### 6.3 Evidence Auditor：证据审计员

核心问题：每项结论能否在输入 SOP 中找到依据？

主要产出：

- 证据引用；
- 无法证明的推断；
- 互相矛盾的规则；
- 关键证据缺口；
- 需要流程负责人补充的信息。

### 6.4 Council Moderator：议会主持人

主持人只基于三份已经验证的专家结果工作，负责：

- 形成共识；
- 保留分歧而不是强行抹平；
- 暴露证据缺口；
- 推荐路径；
- 生成决策和后果；
- 生成结构化 SceneDocument 和报告。

固定执行顺序是：

```text
可选压缩
→ 三名专家并行
→ 三份结果分别校验
→ 主持人汇总
→ 最终结果校验
→ 持久化
→ 用户决策评估
```

系统故意不让 Agent 无限互聊。固定拓扑更容易控制延迟、Token、测试、重试和输出稳定性。

## 7. 与 agency-agents 和 agents-orchestrator 的关系

本项目吸收 `msitarzewski/agency-agents` 的“明确专家职责”思路，以及 agents-orchestrator 中“让多个 Agent 协作并展示过程”的产品方向，但不会把它们原样复制成一个通用 Agent 聊天平台。

SOPscape Council 的比赛实现采用更受控的结构：

- 角色固定为流程、风险、证据三名专家和一名主持人；
- 三名专家并行工作，不进行无限轮对话；
- 主持人只消费三份 Schema 校验通过的结构化结果；
- 共识、分歧和证据缺口会映射成 3D 星图和决策演练；
- 所有角色共用一个受预算、Deadline、AbortSignal 和持久化规则约束的 Core。

因此，这两个项目是角色设计和编排体验的参考，不是当前已经安装的运行时依赖。真正复用任何源码前必须单独核验许可证、依赖、Prompt 质量、Token 成本和与本项目 Schema 的兼容性；在核验完成前，团队不得在演示中声称已经集成其代码。

## 8. 为什么需要 3D

核心原则：**3D 是分析结果和决策后果的可视化界面，不是与业务无关的背景动画。**

| 3D 元素 | 表达的业务含义 |
|---|---|
| 三个专家实体 | 三条真实 Agent 执行状态和各自置信度 |
| 中央共识核心 | 已形成共识的结论 |
| 分歧连线 | 专家仍不一致的判断 |
| 证据节点 | 有原始 SOP 支持的结论 |
| 证据空洞 | 缺少依据、需要人工补充的内容 |
| 风险路径 | 错误选择形成的失败链路与严重程度 |
| 决策节点 | 用户能够采取的业务动作 |
| 镜头、拓扑和颜色变化 | 用户选择后的真实领域状态变化 |

领域数据只能通过 `SceneAdapter.apply(readonlySnapshot)` 映射到 Three.js。Three.js 保持一个渲染循环；GSAP 只负责编排镜头、DOM 和对象参数的时序；React DOM 负责输入、文字、按钮、键盘和无障碍。

即使用户开启 reduced-motion 或设备性能较低，核心含义仍需通过 DOM、颜色、标签和静态空间状态完整表达。

## 9. Web、A2MCP 和 MCP 的关系

```text
Web：面向人类的沉浸式操作 ───────┐
OKX.AI A2MCP：比赛平台调用 ──────┼→ 同一个 SOPscape Core → PostgreSQL
标准 MCP：AI 客户端和工具调用 ───┘
```

三者不是三套产品，也不能把一种协议伪装成另一种协议。它们共享 Core、数据库和领域 Schema，但保留不同的传输、权限、错误和会话语义。

### 9.1 Web

面向人类用户，提供：

- SOP 提交；
- 真实进度；
- 3D 指挥室；
- 决策；
- 报告；
- Owner 管理；
- 只读分享。

### 9.2 OKX.AI A2MCP

这是参加 OKX.AI Genesis Hackathon 的上架接口：

- 公开 HTTPS；
- 比赛期间免费；
- 成功时同步返回 HTTP 200；
- 使用平台审核时冻结的请求和结果 Schema；
- 58 秒绝对入口期限，避免被上游约 65 秒的超时直接切断；
- 比赛部署不安装、不触发 x402 或支付中间件。

### 9.3 标准 MCP

标准 MCP 让支持 MCP 的 AI 客户端调用相同能力：

- Streamable HTTP，统一路径 `/mcp`；
- Tools：生成、读取、评估决策；
- Resources：Scene、Report 和示例；
- Prompts：创建演练、审查证据、辅导学习者；
- Progress：按调用方 Token 发送单调进度；
- Session：初始化、版本、过期、取消、删除和重启失效。

A2MCP 与标准 MCP 不是同一种协议。两者只共享业务 Core，DTO、错误和传输规则分别实现。

### 9.4 Core

所有入口最终只能调用同一个 Core。适配器不得自己：

- 解析 SOP；
- 调用模型；
- 执行专家或主持人；
- 持久化领域结果；
- 计算决策后果。

这一边界保证同一个 Rehearsal 在 Web、A2MCP 和 MCP 中拥有一致的 ID、Schema Version 和业务含义。

## 10. 数据与安全模型

### 10.1 不保存的内容

默认不持久化：

- 原始 SOP；
- Prompt；
- 模型原始响应；
- 思维链；
- 原始 Owner/Share Capability；
- 原始 MCP Session ID；
- API Key 和数据库密钥；
- IP 地址。

数据库只保存输入摘要、清理后的标题、结构化结果、决策、状态和安全运营元数据。

### 10.2 Owner 和 Share 权限

- 创建 Web Rehearsal 时返回一次性的 256-bit Owner Capability；
- 服务端只保存其 SHA-256 哈希；
- Rehearsal UUID 只用于标识，不具备权限；
- Owner 可以查看状态、决策、取消和管理分享；
- 分享使用独立的 192-bit Share Capability；
- Share 只读、可撤销、会过期，不能升级为 Owner。

Owner Capability 不出现在 URL、日志、Metric、Trace 或后续响应中。

## 11. 性能与稳定性如何控制

### 11.1 模型预算

- 可选压缩：1 次，最多 1,200 输出 Token，不重试；
- 三名专家：每人最多 2 次，每次最多 1,200；
- 主持人：最多 2 次，每次最多 2,000；
- 含压缩的绝对上限：9 次 Provider 调用、12,400 个预留输出 Token。

失败、取消、格式错误和超时的已启动尝试都消耗完整预留配额。Schema 修复就是唯一一次重试，禁止隐藏的第三次调用。

### 11.2 生成目标

20 次代表性运行，包括 5 次 15k 中文 SOP：

- 中位数不超过 20 秒；
- p95 不超过 45 秒；
- 所有任务在 60 秒内进入终态；
- A2MCP 必须在 58 秒绝对入口期限内完成响应或固定超时错误。

### 11.3 Web 性能目标

- 桌面 Shell：五次冷导航中位数不超过 2.0 秒，最差不超过 2.5 秒；
- 桌面 30 秒主流程：中位数至少 55 FPS，1% Low 至少 45；
- 指定移动设备：中位数至少 45 FPS，1% Low 至少 35；
- 五轮演练重置后，GPU 资源量回到 Shell 基线的 5% 范围。

移动端允许降低 DPR、阴影、粒子、后处理和镜头复杂度，但不能删除任何核心操作。

## 12. 比赛版范围

比赛 Release Candidate 必须同时具备：

- 免费 A2MCP；
- 三专家和主持人；
- 完整标准 MCP；
- 固定但具有因果表达的 3D 指挥室；
- 钓鱼邮件端到端流程；
- 三个 React Bits 与三个 21st.dev 源码组件；
- 报告和只读分享；
- 桌面、移动、无障碍、协议、安全和性能证据；
- 不超过 90 秒且没有虚假表述的演示。

“早期 A2MCP 纵向切片”只是为了尽早送审，不是最终 MVP。缺少完整 MCP、三专家、六组件或因果 3D 的构建只能标记为未完成，不能包装成比赛 MVP。

## 13. 赛后商业化边界

比赛期间 A2MCP 免费，成功直接返回 HTTP 200，不返回 402，不安装支付 SDK。

赛后收费只在 A2MCP 路由外层加入 OKX Payment SDK 中间件：

```text
OKX Payment Middleware（赛后）
→ A2MCP Adapter
→ SOPscape Core
```

Core、Agent 管线、Web 和标准 MCP 不因收费而重写。自行实现完整 EIP-3009/x402 服务端不在比赛范围内。

## 14. 当前不做什么

为了在截止时间内把核心体验做好，当前不实现：

- 完整账号体系；
- 多人实时协作；
- 生产计费后台；
- 动态生成完全不同的 3D 房间和角色；
- 模型生成任意 3D 资产或代码；
- 多模型市场和自由切换；
- 无限 Agent 讨论；
- 多个完整行业演练世界。

这些不是被遗忘，而是当前没有被验证为比赛交付所必需。

## 15. 两名开发者如何共同实现

### 开发者 A：后端与协议

- Contracts 和 Fixture；
- Core 和 Agent 编排；
- PostgreSQL；
- Web API 与 Capability；
- A2MCP 和部署；
- 标准 MCP；
- 后端安全、时限和资源隔离。

### 开发者 B：产品体验

- React 和 Tailwind；
- Three.js 和 SceneAdapter；
- GSAP Director；
- 六个外部 UI 组件；
- 决策、报告和分享体验；
- 浏览器 E2E、无障碍和前端性能。

第一步不是各自开始写页面和接口，而是先由 A 提交 Contracts 与 Fixture。B 基于固定 Fixture 开发 Web；A 基于同一 Schema 实现 Core 和协议。这样双方可以并行，又不需要同时修改同一批文件。

详细 Git、Worktree、Hunk 和 PR 流程见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 16. 如何判断项目成功

### 16.1 业务成功

用户能无刷新完成：

```text
提交 SOP
→ 观察三专家
→ 理解共识/分歧/证据缺口
→ 作出决策
→ 看到业务状态和 3D 后果
→ 查看报告
→ 创建和撤销只读分享
```

### 16.2 AI 与协议成功

- 15k 中文 SOP 可以进入完整管线；
- 三专家并行且三份结果齐全后才主持；
- Web/A2MCP/MCP 的领域结果一致；
- A2MCP 公网 HTTPS 成功返回 HTTP 200 且无支付；
- MCP Tools/Resources/Prompts/Progress/Session 全部通过正式协议验证。

### 16.3 体验与质量成功

- 达到第 11 节的性能门槛；
- 键盘和 reduced-motion 可完成核心流程；
- 指定模块 lines/branches/functions/statements 均不低于 80%；
- 没有 Critical/High 安全缺陷；
- 日志、数据库和前端 Bundle 不泄漏敏感数据。

### 16.4 比赛资格成功

内部测试通过只证明系统准备完成。ASP 是否已提交、审核、批准和上线必须由 OKX.AI 官方回执证明；X 帖子和最终表单也必须单独记录。没有回执时不能在文档或演示中声称已经上线。

## 17. 常见误解

### “这就是一个 3D 网页吗？”

不是。3D 必须表达真实的专家状态、证据、风险和决策后果；关闭动画后业务含义仍然存在。

### “三个 Agent 是三个聊天人格吗？”

不是。它们是三个固定职责、输出受 Schema 约束的并行分析器。

### “钓鱼邮件是唯一用途吗？”

不是。它是比赛期唯一要求完整打磨的代表场景，Core 的结构可以迁移到其他 SOP。

### “A2MCP 和 MCP 是一回事吗？”

不是。两者共享 Core，但拥有不同协议、DTO、错误、会话和审核要求。

### “标准 MCP 就是增加一个 REST 路由吗？”

不是。完整 MCP 包含初始化、协议版本、Streamable HTTP、会话、Tools、Resources、Prompts、Progress、取消和终止语义。

### “模型会直接生成 3D 代码吗？”

不会，也不允许。模型只输出白名单化、Schema 校验通过的场景数据。

### “分享链接里放 Rehearsal ID 就可以吗？”

不可以。UUID 不是权限；Owner 和只读 Share 使用不同的随机 Capability。

### “比赛现在就要收费吗？”

当前产品决策是比赛期免费，赛后才在 A2MCP 外层接入 OKX Payment SDK。

### “漂亮的 Demo 就等于完成吗？”

不等于。协议、安全、性能、无障碍、外部审核回执和可复现证据都是独立门槛。

### “失败时可以少跑一个专家或者做薄 MCP 吗？”

不允许。用户已确认 no-fallback；运行错误可以安全终止，但不能静默减少产品范围。

## 18. 一分钟复述

用户把 SOP 交给系统；三名专家分别检查流程、风险和证据；主持人在三份有效结果上保留共识与分歧并生成可演练决策；用户的选择同时改变后端结果和 3D 后果；同一个 Core 被 Web、OKX.AI A2MCP 和标准 MCP 以不同协议调用；比赛期间免费，赛后才在 A2MCP 外层接入支付。
